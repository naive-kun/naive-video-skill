interface:
  display_name: "Naive Video Skill"
  short_description: "From talking-head footage to captions, motion preview, and final export."
  default_prompt: "Use $talking-head-video-pipeline to initialize my video project, guide me through captions and style, build an official preview, and export only after I approve it."
policy:
  allow_implicit_invocation: true
