interface:
  display_name: "Naive Video Skill"
  short_description: "Beginner-first rough cut, captions, curated semantic motion, preview, and export."
  default_prompt: "Use $talking-head-video-pipeline as a step-by-step mentor: check whether my footage needs an optional rough cut, guide captions, content logic and asset placement, apply the curated native HyperFrames + GSAP shot pack where semantics and safe zones allow, review representative keyframes, build an official motion preview, and export only after I approve it. Never install ShotCraft or Remotion without asking me first."
policy:
  allow_implicit_invocation: true
